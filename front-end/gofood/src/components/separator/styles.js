import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 1,
    overflow: 'hidden',
  },
  borderContainer: {
    borderWidth: 1,
  },
});

export default styles;
